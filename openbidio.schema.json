{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openbidio.dev/schema/0.3/openbidio.schema.json",
  "title": "OpenBidIO bid document, v0.3 (draft)",
  "type": "object",
  "additionalProperties": false,
  "required": ["bidio", "id", "conformance", "project", "currency", "shots", "totals"],
  "properties": {
    "bidio": { "type": "string", "pattern": "^0\\.3(\\.\\d+)?$" },
    "id": { "type": "string", "format": "uuid" },
    "bid_id": { "type": "string", "format": "uuid" },
    "conformance": { "enum": ["M1", "M1-Multisite", "M1-Series", "M1-Full"] },
    "generator": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "version": { "type": "string" }
      }
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" },
    "project": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title"],
      "properties": {
        "title": { "type": "string", "minLength": 1 },
        "code": { "type": "string" },
        "client": { "type": "string" },
        "kind": { "type": "string" }
      }
    },
    "parties": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "vendor": { "$ref": "#/$defs/party" },
        "client": { "$ref": "#/$defs/party" }
      }
    },
    "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
    "fx_rates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["currency", "rate"],
        "properties": {
          "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
          "rate": { "type": "number", "exclusiveMinimum": 0 }
        }
      }
    },
    "sites": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["key", "jurisdiction"],
        "properties": {
          "key": { "$ref": "#/$defs/siteKey" },
          "label": { "type": "string" },
          "jurisdiction": { "type": "string", "minLength": 2 },
          "rate_card": { "$ref": "#/$defs/rateCard" }
        }
      }
    },
    "episodes": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["code"],
        "properties": {
          "code": { "$ref": "#/$defs/episodeCode" },
          "title": { "type": "string" },
          "frames": {
            "type": "object",
            "additionalProperties": false,
            "properties": { "count": { "type": "integer", "minimum": 0 } }
          }
        }
      }
    },
    "departments": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["key"],
        "properties": {
          "key": { "$ref": "#/$defs/deptKey" },
          "label": { "type": "string" }
        }
      }
    },
    "shot_types": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["key"],
        "properties": {
          "key": { "type": "string", "minLength": 1 },
          "label": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    },
    "rate_card": { "$ref": "#/$defs/rateCard" },
    "overheads": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["key", "rate"],
        "properties": {
          "key": { "type": "string", "pattern": "^[a-z][a-z0-9_-]*$" },
          "label": { "type": "string" },
          "rate": { "type": "number", "minimum": 0, "maximum": 1 },
          "extensions": { "$ref": "#/$defs/extensions" }
        }
      }
    },
    "shots": {
      "type": "array",
      "items": { "$ref": "#/$defs/shot" }
    },
    "line_items": {
      "type": "array",
      "items": { "$ref": "#/$defs/lineItem" }
    },
    "incentives": {
      "type": "array",
      "items": { "$ref": "#/$defs/incentive" }
    },
    "references": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "uri"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "uri": { "type": "string", "minLength": 1 },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
          "kind": { "type": "string" }
        }
      }
    },
    "totals": {
      "type": "object",
      "additionalProperties": false,
      "required": ["gross", "incentive_credit", "net"],
      "properties": {
        "shots_subtotal": { "type": "number" },
        "line_items_subtotal": { "type": "number" },
        "discount_total": { "type": "number" },
        "overhead_total": { "type": "number" },
        "gross": { "type": "number" },
        "incentive_credit": { "type": "number" },
        "net": { "type": "number" },
        "by_site": {
          "type": "object",
          "additionalProperties": false,
          "patternProperties": {
            "^[a-z][a-z0-9_-]*$": { "$ref": "#/$defs/totalsBlock" }
          }
        },
        "by_episode": {
          "type": "object",
          "additionalProperties": false,
          "patternProperties": {
            "^[A-Za-z0-9][A-Za-z0-9_-]*$": { "$ref": "#/$defs/totalsBlock" }
          }
        }
      }
    },
    "revision": {
      "type": "object",
      "additionalProperties": false,
      "required": ["number"],
      "properties": {
        "number": { "type": "integer", "minimum": 1 },
        "variant": { "type": "string", "minLength": 1 },
        "locked": { "type": "boolean" },
        "supersedes": { "type": ["string", "null"], "format": "uuid" }
      }
    },
    "award": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status"],
      "properties": {
        "status": { "enum": ["draft", "submitted", "awarded", "declined", "withdrawn"] },
        "submitted_at": { "type": "string", "format": "date-time" },
        "awarded_at": { "type": "string", "format": "date-time" },
        "client_reference": { "type": "string" }
      }
    },
    "extensions": { "$ref": "#/$defs/extensions" }
  },
  "if": {
    "required": ["revision"],
    "properties": { "revision": { "required": ["variant"] } }
  },
  "then": { "required": ["bid_id"] },
  "$defs": {
    "deptKey": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
    "siteKey": { "type": "string", "pattern": "^[a-z][a-z0-9_-]*$" },
    "episodeCode": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$" },
    "rateCard": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^[a-z][a-z0-9_]*$": { "type": "number", "minimum": 0 }
      }
    },
    "party": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "country": { "type": "string", "pattern": "^[A-Z]{2}$" }
      }
    },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*(\\.[a-z][a-z0-9-]*)+$" },
      "additionalProperties": { "type": "object" }
    },
    "totalsBlock": {
      "type": "object",
      "additionalProperties": false,
      "required": ["gross", "incentive_credit", "net"],
      "properties": {
        "shots_subtotal": { "type": "number" },
        "line_items_subtotal": { "type": "number" },
        "gross": { "type": "number" },
        "incentive_credit": { "type": "number" },
        "net": { "type": "number" }
      }
    },
    "shot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "code"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "code": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "sequence": { "type": "string" },
        "type": { "type": "string" },
        "difficulty": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "quantity": { "type": "number", "exclusiveMinimum": 0, "default": 1 },
        "execution_site": { "$ref": "#/$defs/siteKey" },
        "episode": { "$ref": "#/$defs/episodeCode" },
        "frames": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "count": { "type": "integer", "minimum": 0 },
            "in": { "type": "integer" },
            "out": { "type": "integer" }
          }
        },
        "efforts": {
          "type": "object",
          "additionalProperties": false,
          "patternProperties": {
            "^[a-z][a-z0-9_]*$": { "type": "number", "minimum": 0 }
          }
        },
        "unit_price": { "type": ["number", "null"], "minimum": 0 },
        "discount": { "type": "number", "minimum": 0, "maximum": 1 },
        "labour_share": { "type": "number", "minimum": 0, "maximum": 1 },
        "notes": { "type": "string" },
        "extensions": { "$ref": "#/$defs/extensions" }
      }
    },
    "lineItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "quantity", "unit_cost"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "minLength": 1 },
        "kind": { "type": "string" },
        "quantity": { "type": "number", "minimum": 0 },
        "unit": { "type": "string" },
        "unit_cost": { "type": "number", "minimum": 0 },
        "discount": { "type": "number", "minimum": 0, "maximum": 1 },
        "labour_share": { "type": "number", "minimum": 0, "maximum": 1 },
        "execution_site": { "$ref": "#/$defs/siteKey" },
        "episode": { "$ref": "#/$defs/episodeCode" },
        "extensions": { "$ref": "#/$defs/extensions" }
      }
    },
    "incentive": {
      "type": "object",
      "additionalProperties": false,
      "required": ["jurisdiction", "labour_share", "labour_rate", "nonlabour_rate"],
      "properties": {
        "jurisdiction": { "type": "string", "minLength": 2 },
        "program": { "type": "string" },
        "sites": {
          "type": "array",
          "items": { "$ref": "#/$defs/siteKey" },
          "minItems": 1
        },
        "labour_share": { "type": "number", "minimum": 0, "maximum": 1 },
        "labour_rate": { "type": "number", "minimum": 0, "maximum": 1 },
        "nonlabour_rate": { "type": "number", "minimum": 0, "maximum": 1 },
        "extensions": { "$ref": "#/$defs/extensions" }
      }
    }
  }
}
